spring:
  application:
    name: ToTheStars
  flyway:
    enabled: false

  datasource:
    url: jdbc:postgresql://localhost:5432/tothestars
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true